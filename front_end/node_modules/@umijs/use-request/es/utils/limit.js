export default function limit(fn, timespan) {
  var pending = false;
  return function () {
    if (pending) return;
    pending = true;
    fn.apply(void 0, arguments);
    setTimeout(function () {
      pending = false;
    }, timespan);
  };
}